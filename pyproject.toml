[project]
name = "catalyst"
version = "0.1.0"
description = ""
authors = [
    {name = "fcas",email = "f3l1nux@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy (>=2.5.2,<3.0.0)",
    "torch (>=2.13.0,<3.0.0)",
    "accelerate (>=1.14.0,<2.0.0)",
    "hydra-slayer (>=0.5.0,<0.6.0)",
    "tqdm (>=4.70.0,<5.0.0)",
    "tensorboardx (>=2.6.5,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
